<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %> <%= book.title %>  </h1>

    <form action="/books/edit/<%= book.id %> " method="POST">
        <div>
            <label for="">Titulo</label>
            <input type="text" name="title" value="<%= book.title %> " id="">
        </div>
        <div>
            <label for="">Autor</label>
            <input type="text" name="author" value="<%= book.author %> " id="">
        </div>
        <div>
            <label for="">Precio</label>
            <input type="text" name="price" value="<%= book.price %> " id="">
        </div>
        <div>
            <label for="">Descripción</label>
            <input type="text" name="description" value="<%= book.description %> " id="">
        </div>
        <div>
            <label for="">Fecha de Lanzamiento</label>
            <input type="text" name="release_date" value="<%= book.release_date %> " id="">
        </div>
        <div>
            <label for="">Género</label>
            <select name="genre" id="">
    
                <% for( let i = 0; i < genres.length; i++ ) { %>
                    <% if (genres[i].id == book.genre_id) { %>
                        <option value="<%= genres[i].id %>" selected>
                            <%= genres[i].name %> 
                        </option>
                    <% } else { %>
                        <option value="<%= genres[i].id %>">
                            <%= genres[i].name %> 
                        </option>
                    <% } %>
                    
                    
                <% } %>
                
            </select>
        </div>

        <button type="submit">Editar</button>
    </form>

  </body>
</html>